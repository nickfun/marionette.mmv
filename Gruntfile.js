module.exports = function(grunt) {
	grunt.initConfig( {
		pkg: grunt.file.readJSON('package.json'),
		// paths
		path_source: 'src/',
		path_dest:	'build/',
		
		// tasks
		uglify: {
		}
	});

	// import the modules
});
